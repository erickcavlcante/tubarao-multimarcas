# Carrinho Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Um visitante consegue adicionar uma variação de produto ao carrinho pela página do produto, ver o contador de itens no cabeçalho, abrir `/carrinho`, alterar quantidades, remover itens, e ver o subtotal com o quanto falta pro frete grátis.

**Architecture:** O carrinho vive no `localStorage` do navegador (decisão do spec — sem tabela no banco), guardando **apenas** `{ variationId, quantity }`. Um React Context (`CartProvider`) no layout da loja mantém esse estado sincronizado. **Preço e estoque nunca vêm do `localStorage`** — a página do carrinho envia os ids ao servidor via Server Action, que relê tudo do banco e devolve os dados frescos. Isso é a exigência de segurança que o review final do Plano 3 registrou: o cliente pode adulterar qualquer coisa que esteja no navegador dele.

**Tech Stack:** Next.js 16 (App Router, Server Actions), React 19 (Context, `useActionState` não é necessário aqui), Prisma 7

**Spec:** `docs/superpowers/specs/2026-08-18-ecommerce-v1-design.md`

## Escopo: o que NÃO entra neste plano

- **Cálculo de frete por CEP (Melhor Envio)** — fica pro plano seguinte. Depende de uma conta/token do Melhor Envio e de um campo de **peso** nos produtos que ainda não existe no schema. O carrinho deste plano mostra subtotal e progresso pro frete grátis, mas não cota frete.
- **Checkout e pagamento** — plano posterior. O botão "Finalizar compra" aponta pra `/checkout`, que vai dar 404 até lá (esperado, mesmo padrão dos planos anteriores).
- **Limpar o carrinho após a compra** — sem checkout, não há o que limpar ainda. `clear()` existe no contexto pro plano de checkout usar.

## Global Constraints

- Sempre usar o singleton `src/lib/prisma.ts` (`import { prisma } from "@/lib/prisma"`), nunca instanciar `PrismaClient`.
- **Toda rota pública nova precisa de `export const dynamic = "force-dynamic";`** — regra fixa descoberta no Plano 3: sem isso o Next.js 16 pré-renderiza a página como HTML estático no build e ela congela no deploy. Query do Prisma NÃO conta como sinal dinâmico. Verificar depois do build que a rota não aparece em `.next/prerender-manifest.json`.
- **Preço e estoque SEMPRE relidos do banco no servidor.** O `localStorage` guarda só `variationId` e `quantity`. Nenhum valor monetário vindo do cliente pode ser usado em cálculo — só exibido depois de vir do servidor.
- **Só variações de produtos publicamente visíveis entram no carrinho** — `product.active === true`. Um item cujo produto foi desativado depois de adicionado deve sumir do carrinho silenciosamente (com aviso ao usuário), nunca ser comprável.
- Dinheiro em centavos, nunca float — usar `centsToReais`/`applyPixDiscount` de `src/lib/money.ts`.
- Imagens: `<img>` simples, nunca `next/image` (URLs coladas pelo admin, hosts desconhecidos).
- `params`/`searchParams` são `Promise` neste Next.js e `searchParams` entrega `string | string[] | undefined` — nenhuma rota deste plano lê `searchParams`, mas a regra vale se alguma vier a ler.
- **Nunca criar nem commitar `.env.example`.**
- `AGENTS.md` na raiz e `node_modules/next/dist/docs/` são **reais** — um implementador de plano anterior afirmou incorretamente que não existiam. Se algo não compilar como esperado, conferir a doc real antes de assumir que é ruído.

---

### Task 1: Lógica pura do carrinho (com testes)

**Files:**
- Create: `src/lib/cart.ts`
- Test: `src/lib/cart.test.ts`

**Interfaces:**
- Produces: `type CartLine = { variationId: string; quantity: number }`, `MAX_QUANTITY_PER_LINE` (constante), `addLine(lines, variationId, quantity): CartLine[]`, `updateLineQuantity(lines, variationId, quantity): CartLine[]`, `removeLine(lines, variationId): CartLine[]`, `totalQuantity(lines): number`, `parseStoredCart(raw: unknown): CartLine[]`

- [ ] **Step 1: Escrever os testes (devem falhar)**

Create `src/lib/cart.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import {
  addLine,
  updateLineQuantity,
  removeLine,
  totalQuantity,
  parseStoredCart,
  MAX_QUANTITY_PER_LINE,
  type CartLine,
} from "./cart";

describe("addLine", () => {
  it("adds a new line to an empty cart", () => {
    expect(addLine([], "v1", 2)).toEqual([{ variationId: "v1", quantity: 2 }]);
  });

  it("sums the quantity when the variation is already in the cart", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: 2 }];
    expect(addLine(lines, "v1", 3)).toEqual([{ variationId: "v1", quantity: 5 }]);
  });

  it("keeps other lines untouched when adding", () => {
    const lines: CartLine[] = [
      { variationId: "v1", quantity: 1 },
      { variationId: "v2", quantity: 4 },
    ];
    expect(addLine(lines, "v1", 1)).toEqual([
      { variationId: "v1", quantity: 2 },
      { variationId: "v2", quantity: 4 },
    ]);
  });

  it("ignores a non-positive quantity", () => {
    expect(addLine([], "v1", 0)).toEqual([]);
    expect(addLine([], "v1", -3)).toEqual([]);
  });

  it("caps the quantity at MAX_QUANTITY_PER_LINE", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: MAX_QUANTITY_PER_LINE }];
    expect(addLine(lines, "v1", 10)).toEqual([
      { variationId: "v1", quantity: MAX_QUANTITY_PER_LINE },
    ]);
  });

  it("does not mutate the input array", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: 1 }];
    addLine(lines, "v1", 1);
    expect(lines).toEqual([{ variationId: "v1", quantity: 1 }]);
  });
});

describe("updateLineQuantity", () => {
  it("sets a new quantity", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: 2 }];
    expect(updateLineQuantity(lines, "v1", 7)).toEqual([{ variationId: "v1", quantity: 7 }]);
  });

  it("removes the line when the quantity drops to zero or below", () => {
    const lines: CartLine[] = [
      { variationId: "v1", quantity: 2 },
      { variationId: "v2", quantity: 1 },
    ];
    expect(updateLineQuantity(lines, "v1", 0)).toEqual([{ variationId: "v2", quantity: 1 }]);
    expect(updateLineQuantity(lines, "v1", -5)).toEqual([{ variationId: "v2", quantity: 1 }]);
  });

  it("caps at MAX_QUANTITY_PER_LINE", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: 1 }];
    expect(updateLineQuantity(lines, "v1", MAX_QUANTITY_PER_LINE + 50)).toEqual([
      { variationId: "v1", quantity: MAX_QUANTITY_PER_LINE },
    ]);
  });
});

describe("removeLine", () => {
  it("removes only the matching variation", () => {
    const lines: CartLine[] = [
      { variationId: "v1", quantity: 2 },
      { variationId: "v2", quantity: 1 },
    ];
    expect(removeLine(lines, "v1")).toEqual([{ variationId: "v2", quantity: 1 }]);
  });

  it("is a no-op when the variation is not in the cart", () => {
    const lines: CartLine[] = [{ variationId: "v1", quantity: 2 }];
    expect(removeLine(lines, "v9")).toEqual(lines);
  });
});

describe("totalQuantity", () => {
  it("returns zero for an empty cart", () => {
    expect(totalQuantity([])).toBe(0);
  });

  it("sums quantities across lines", () => {
    expect(
      totalQuantity([
        { variationId: "v1", quantity: 2 },
        { variationId: "v2", quantity: 3 },
      ])
    ).toBe(5);
  });
});

describe("parseStoredCart", () => {
  it("returns an empty array for anything that is not an array", () => {
    expect(parseStoredCart(null)).toEqual([]);
    expect(parseStoredCart("lixo")).toEqual([]);
    expect(parseStoredCart({ variationId: "v1" })).toEqual([]);
  });

  it("keeps only well-formed lines", () => {
    const raw = [
      { variationId: "v1", quantity: 2 },
      { variationId: "v2" },
      { quantity: 3 },
      { variationId: 5, quantity: 3 },
      { variationId: "v3", quantity: "muitos" },
      { variationId: "v4", quantity: 1 },
    ];
    expect(parseStoredCart(raw)).toEqual([
      { variationId: "v1", quantity: 2 },
      { variationId: "v4", quantity: 1 },
    ]);
  });

  it("drops lines with a non-positive quantity", () => {
    expect(parseStoredCart([{ variationId: "v1", quantity: 0 }])).toEqual([]);
    expect(parseStoredCart([{ variationId: "v1", quantity: -2 }])).toEqual([]);
  });
});
```

- [ ] **Step 2: Rodar os testes e confirmar que falham**

Run: `npm test`
Expected: FAIL — `src/lib/cart.ts` não existe ainda

- [ ] **Step 3: Implementar**

Create `src/lib/cart.ts`:
```typescript
export type CartLine = { variationId: string; quantity: number };

export const MAX_QUANTITY_PER_LINE = 99;

function cap(quantity: number): number {
  return Math.min(quantity, MAX_QUANTITY_PER_LINE);
}

export function addLine(lines: CartLine[], variationId: string, quantity: number): CartLine[] {
  if (quantity <= 0) {
    return lines;
  }
  const existing = lines.find((l) => l.variationId === variationId);
  if (!existing) {
    return [...lines, { variationId, quantity: cap(quantity) }];
  }
  return lines.map((l) =>
    l.variationId === variationId ? { ...l, quantity: cap(l.quantity + quantity) } : l
  );
}

export function updateLineQuantity(
  lines: CartLine[],
  variationId: string,
  quantity: number
): CartLine[] {
  if (quantity <= 0) {
    return removeLine(lines, variationId);
  }
  return lines.map((l) =>
    l.variationId === variationId ? { ...l, quantity: cap(quantity) } : l
  );
}

export function removeLine(lines: CartLine[], variationId: string): CartLine[] {
  return lines.filter((l) => l.variationId !== variationId);
}

export function totalQuantity(lines: CartLine[]): number {
  return lines.reduce((sum, l) => sum + l.quantity, 0);
}

export function parseStoredCart(raw: unknown): CartLine[] {
  if (!Array.isArray(raw)) {
    return [];
  }
  return raw.filter(
    (l): l is CartLine =>
      typeof l === "object" &&
      l !== null &&
      typeof (l as CartLine).variationId === "string" &&
      typeof (l as CartLine).quantity === "number" &&
      Number.isFinite((l as CartLine).quantity) &&
      (l as CartLine).quantity > 0
  );
}
```

- [ ] **Step 4: Rodar os testes e confirmar que passam**

Run: `npm test`
Expected: PASS — 16 testes anteriores + os novos deste arquivo

- [ ] **Step 5: Commit**

```bash
git add src/lib/cart.ts src/lib/cart.test.ts
git commit -m "feat: add pure cart line manipulation logic"
```

---

### Task 2: CartProvider (contexto) e contador no cabeçalho

**Files:**
- Create: `src/app/(loja)/_components/CartProvider.tsx`
- Create: `src/app/(loja)/_components/CartLink.tsx`
- Modify: `src/app/(loja)/layout.tsx` (envolver `children` no `CartProvider`, trocar a nav pra incluir o `CartLink`)

**Interfaces:**
- Consumes: `addLine`, `updateLineQuantity`, `removeLine`, `totalQuantity`, `parseStoredCart`, `CartLine` de `src/lib/cart.ts` (Task 1)
- Produces: `CartProvider` (componente), `useCart(): { lines: CartLine[]; ready: boolean; count: number; addItem(variationId, quantity): void; updateItem(variationId, quantity): void; removeItem(variationId): void; clear(): void }` — consumido pelas Tasks 4 e 5

- [ ] **Step 1: Criar o CartProvider**

Create `src/app/(loja)/_components/CartProvider.tsx`:
```tsx
"use client";

import { createContext, useContext, useEffect, useState } from "react";
import {
  addLine,
  updateLineQuantity,
  removeLine,
  totalQuantity,
  parseStoredCart,
  type CartLine,
} from "@/lib/cart";

const STORAGE_KEY = "loja-carrinho";

type CartContextValue = {
  lines: CartLine[];
  ready: boolean;
  count: number;
  addItem: (variationId: string, quantity: number) => void;
  updateItem: (variationId: string, quantity: number) => void;
  removeItem: (variationId: string) => void;
  clear: () => void;
};

const CartContext = createContext<CartContextValue | null>(null);

export function CartProvider({ children }: { children: React.ReactNode }) {
  const [lines, setLines] = useState<CartLine[]>([]);
  const [ready, setReady] = useState(false);

  useEffect(() => {
    try {
      const raw = window.localStorage.getItem(STORAGE_KEY);
      if (raw) {
        setLines(parseStoredCart(JSON.parse(raw)));
      }
    } catch {
      // localStorage indisponível ou conteúdo inválido — começa vazio
    }
    setReady(true);
  }, []);

  useEffect(() => {
    if (!ready) {
      return;
    }
    try {
      window.localStorage.setItem(STORAGE_KEY, JSON.stringify(lines));
    } catch {
      // modo privado ou sem espaço — o carrinho continua funcionando na sessão
    }
  }, [lines, ready]);

  const value: CartContextValue = {
    lines,
    ready,
    count: totalQuantity(lines),
    addItem: (variationId, quantity) => setLines((prev) => addLine(prev, variationId, quantity)),
    updateItem: (variationId, quantity) =>
      setLines((prev) => updateLineQuantity(prev, variationId, quantity)),
    removeItem: (variationId) => setLines((prev) => removeLine(prev, variationId)),
    clear: () => setLines([]),
  };

  return <CartContext.Provider value={value}>{children}</CartContext.Provider>;
}

export function useCart(): CartContextValue {
  const ctx = useContext(CartContext);
  if (!ctx) {
    throw new Error("useCart precisa ser usado dentro de um CartProvider");
  }
  return ctx;
}
```

Nota sobre o `ready`: o servidor renderiza o carrinho vazio (não tem acesso ao `localStorage`), e o cliente só carrega o conteúdo real depois de montar. O `ready` evita mostrar "(0)" no primeiro render e depois piscar pro número certo, e evita erro de hidratação.

- [ ] **Step 2: Criar o link do carrinho com contador**

Create `src/app/(loja)/_components/CartLink.tsx`:
```tsx
"use client";

import Link from "next/link";
import { useCart } from "./CartProvider";

export function CartLink() {
  const { count, ready } = useCart();
  return <Link href="/carrinho">Carrinho{ready && count > 0 ? ` (${count})` : ""}</Link>;
}
```

- [ ] **Step 3: Atualizar o layout da loja**

Modify `src/app/(loja)/layout.tsx`:

1. Adicionar aos imports do topo:
```tsx
import { CartProvider } from "./_components/CartProvider";
import { CartLink } from "./_components/CartLink";
```

2. Trocar o bloco `<nav>` existente (que hoje tem só o link "Todos os produtos") por:
```tsx
          <nav style={{ display: "flex", gap: 16 }}>
            <Link href="/produtos">Todos os produtos</Link>
            <CartLink />
          </nav>
```

3. Envolver o conteúdo retornado no `CartProvider` — a `<div>` externa inteira do return vira filha dele:
```tsx
  return (
    <CartProvider>
      <div>
        {/* ...header, main, footer exatamente como já estão... */}
      </div>
    </CartProvider>
  );
```

- [ ] **Step 4: Verificar manualmente**

Run: `npm run dev`. Acessar `/` — o cabeçalho deve mostrar "Carrinho" (sem número, carrinho vazio) ao lado de "Todos os produtos". Abrir o console do navegador e não deve haver erro de hidratação. Clicar em "Carrinho" leva a `/carrinho`, que ainda dá 404 até a Task 5 (esperado).

- [ ] **Step 5: Commit**

```bash
git add src/app/\(loja\)/_components/CartProvider.tsx src/app/\(loja\)/_components/CartLink.tsx src/app/\(loja\)/layout.tsx
git commit -m "feat: add cart context provider and header cart link"
```

---

### Task 3: Leitura do carrinho no servidor (preço e estoque frescos do banco)

**Files:**
- Modify: `src/lib/catalog.ts` (adicionar `getPublicVariations`)
- Create: `src/app/(loja)/carrinho/actions.ts`

**Interfaces:**
- Consumes: `prisma` de `src/lib/prisma.ts`, `CartLine` de `src/lib/cart.ts` (Task 1)
- Produces: `getPublicVariations(variationIds: string[])` em `src/lib/catalog.ts`
- Produces: `type CartItem`, `type LoadedCart`, `loadCart(lines: CartLine[]): Promise<LoadedCart>` em `src/app/(loja)/carrinho/actions.ts` — consumido pela Task 5

Esta é a task mais importante do plano em termos de segurança. O que chega do cliente é **só** uma lista de `{ variationId, quantity }`, e nem isso é confiável: os ids podem apontar pra produtos desativados, e as quantidades podem exceder o estoque ou ser absurdas. Tudo que envolve dinheiro (`priceCents`, subtotal) é lido do banco aqui e nunca aceito do cliente.

- [ ] **Step 1: Adicionar `getPublicVariations` ao catalog**

Modify `src/lib/catalog.ts` — adicionar ao final do arquivo (mantendo tudo que já existe intocado):

```typescript
export async function getPublicVariations(variationIds: string[]) {
  if (variationIds.length === 0) {
    return [];
  }
  return prisma.productVariation.findMany({
    where: {
      id: { in: variationIds },
      product: { active: true },
    },
    include: { product: true },
  });
}
```

- [ ] **Step 2: Criar a Server Action de leitura do carrinho**

Create `src/app/(loja)/carrinho/actions.ts`:
```typescript
"use server";

import { getPublicVariations } from "@/lib/catalog";
import { MAX_QUANTITY_PER_LINE, type CartLine } from "@/lib/cart";

const MAX_LINES = 100;

export type CartItem = {
  variationId: string;
  productName: string;
  productSlug: string;
  size: string;
  color: string;
  image: string | null;
  priceCents: number;
  quantity: number;
  availableStock: number;
  lineTotalCents: number;
  adjusted: boolean;
};

export type LoadedCart = {
  items: CartItem[];
  subtotalCents: number;
  droppedCount: number;
};

export async function loadCart(lines: CartLine[]): Promise<LoadedCart> {
  const safeLines = (Array.isArray(lines) ? lines : [])
    .filter(
      (l) =>
        typeof l?.variationId === "string" &&
        typeof l?.quantity === "number" &&
        Number.isFinite(l.quantity) &&
        l.quantity > 0
    )
    .slice(0, MAX_LINES);

  // Soma linhas repetidas da mesma variação ANTES de limitar pelo estoque.
  // Sem isso, N linhas do mesmo item passariam cada uma pelo teto do estoque
  // cheio, e o carrinho poderia representar N x o estoque real.
  const requestedByVariation = new Map<string, number>();
  for (const line of safeLines) {
    const previous = requestedByVariation.get(line.variationId) ?? 0;
    requestedByVariation.set(line.variationId, previous + Math.floor(line.quantity));
  }

  const variations = await getPublicVariations([...requestedByVariation.keys()]);
  const byId = new Map(variations.map((v) => [v.id, v]));

  const items: CartItem[] = [];
  for (const [variationId, rawQuantity] of requestedByVariation) {
    const variation = byId.get(variationId);
    if (!variation) {
      continue;
    }
    const requested = Math.min(rawQuantity, MAX_QUANTITY_PER_LINE);
    const quantity = Math.min(requested, variation.stock);
    if (quantity <= 0) {
      continue;
    }
    items.push({
      variationId: variation.id,
      productName: variation.product.name,
      productSlug: variation.product.slug,
      size: variation.size,
      color: variation.color,
      image: variation.image ?? variation.product.images[0] ?? null,
      priceCents: variation.priceCents,
      quantity,
      availableStock: variation.stock,
      lineTotalCents: variation.priceCents * quantity,
      adjusted: quantity !== requested,
    });
  }

  return {
    items,
    subtotalCents: items.reduce((sum, i) => sum + i.lineTotalCents, 0),
    droppedCount: requestedByVariation.size - items.length,
  };
}
```

Notas:
- `loadCart` é público de propósito — **não** chamar `requireAdmin()` aqui (isso é uma página de loja, não do admin).
- **A soma por variação antes do teto de estoque é obrigatória**, não uma otimização: sem ela, um cliente que mande a mesma variação em várias linhas (possível mandando dados direto pro servidor, sem passar pela interface) recebe um carrinho com várias vezes o estoque real. O checkout vai confiar nesta função como fonte da verdade.
- `adjusted: true` marca um item cuja quantidade foi reduzida por falta de estoque, pra Task 5 avisar o usuário.
- `droppedCount` conta variações que sumiram (produto desativado, variação excluída, ou estoque zerado), também pra avisar.

- [ ] **Step 3: Commit**

```bash
git add src/lib/catalog.ts src/app/\(loja\)/carrinho/actions.ts
git commit -m "feat: add server-side cart loading with fresh prices and stock"
```

---

### Task 4: Botão "Adicionar ao carrinho" na página do produto

**Files:**
- Modify: `src/app/(loja)/produto/[slug]/VariationSelector.tsx`

**Interfaces:**
- Consumes: `useCart` de `src/app/(loja)/_components/CartProvider.tsx` (Task 2)

- [ ] **Step 1: Atualizar o VariationSelector**

Modify `src/app/(loja)/produto/[slug]/VariationSelector.tsx`:

1. Adicionar aos imports do topo (junto com o `useState` que já é importado de `react`):
```tsx
import Link from "next/link";
import { useCart } from "../../_components/CartProvider";
```

(Import relativo, não `@/app/(loja)/...` — o alias `@/` funciona, mas caminhos com parênteses de route group são frágeis em imports; o projeto já usa o padrão relativo pra `_components`, ex: `../../_components/ProductCard` na página de categoria.)

2. Dentro do componente, logo depois da linha `const selected = variations.find((v) => v.id === selectedId)!;`, adicionar:
```tsx
  const { addItem } = useCart();
  const [quantity, setQuantity] = useState(1);
  const [added, setAdded] = useState(false);

  function handleAddToCart() {
    addItem(selected.id, quantity);
    setAdded(true);
  }
```

3. Trocar o `onClick` dos botões de variação pra também limpar o aviso de "adicionado" ao trocar de variação — a linha `onClick={() => setSelectedId(v.id)}` vira:
```tsx
            onClick={() => {
              setSelectedId(v.id);
              setAdded(false);
            }}
```

4. Adicionar, logo antes do fechamento `</div>` final do componente (depois do parágrafo que mostra o estoque):
```tsx
      {selected.stock > 0 && (
        <div style={{ marginTop: 16 }}>
          <label>
            Quantidade:{" "}
            <input
              type="number"
              min={1}
              max={selected.stock}
              value={quantity}
              onChange={(e) => {
                const raw = Number(e.target.value);
                const next = Number.isFinite(raw) ? Math.floor(raw) : 1;
                setQuantity(Math.min(Math.max(next, 1), selected.stock));
                setAdded(false);
              }}
              style={{ width: 60 }}
            />
          </label>
          <button
            type="button"
            onClick={handleAddToCart}
            style={{ marginLeft: 8, padding: "8px 16px" }}
          >
            Adicionar ao carrinho
          </button>
          {added && (
            <p>
              Adicionado ao carrinho. <Link href="/carrinho">Ver carrinho</Link>
            </p>
          )}
        </div>
      )}
```

- [ ] **Step 2: Verificar manualmente**

Run: `npm run dev`. Abrir a página de um produto real com estoque. Escolher uma variação, ajustar a quantidade, clicar em "Adicionar ao carrinho" — deve aparecer a mensagem de confirmação e o contador do cabeçalho deve subir imediatamente (sem recarregar a página). Recarregar a página (F5) — o contador deve **continuar** com o valor certo (veio do `localStorage`). Adicionar a mesma variação de novo deve somar à quantidade existente, não criar uma linha nova. Trocar pra uma variação esgotada deve esconder o bloco de quantidade/botão.

- [ ] **Step 3: Commit**

```bash
git add src/app/\(loja\)/produto/\[slug\]/VariationSelector.tsx
git commit -m "feat: add add-to-cart control on product page"
```

---

### Task 5: Página do carrinho (`/carrinho`)

**Files:**
- Create: `src/app/(loja)/carrinho/page.tsx`
- Create: `src/app/(loja)/carrinho/CartView.tsx`

**Interfaces:**
- Consumes: `useCart` de `src/app/(loja)/_components/CartProvider.tsx` (Task 2), `loadCart`/`CartItem`/`LoadedCart` de `src/app/(loja)/carrinho/actions.ts` (Task 3), `centsToReais`/`applyPixDiscount` de `src/lib/money.ts`, `prisma` de `src/lib/prisma.ts`

- [ ] **Step 1: Criar a view do carrinho (client component)**

Create `src/app/(loja)/carrinho/CartView.tsx`:
```tsx
"use client";

import { useEffect, useState } from "react";
import Link from "next/link";
import { useCart } from "../_components/CartProvider";
import { loadCart, type LoadedCart } from "./actions";
import { centsToReais, applyPixDiscount } from "@/lib/money";

export function CartView({
  freeShippingCents,
  pixDiscountPercent,
}: {
  freeShippingCents: number;
  pixDiscountPercent: number;
}) {
  const { lines, ready, updateItem, removeItem } = useCart();
  const [cart, setCart] = useState<LoadedCart | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    if (!ready) {
      return;
    }
    let cancelled = false;
    setLoading(true);
    loadCart(lines)
      .then((result) => {
        if (!cancelled) {
          setCart(result);
        }
      })
      .finally(() => {
        if (!cancelled) {
          setLoading(false);
        }
      });
    return () => {
      cancelled = true;
    };
  }, [lines, ready]);

  if (!ready || loading) {
    return <p>Carregando carrinho...</p>;
  }

  if (!cart || cart.items.length === 0) {
    return (
      <div>
        <p>Seu carrinho está vazio.</p>
        <Link href="/produtos">Ver produtos</Link>
      </div>
    );
  }

  const missingForFreeShipping = freeShippingCents - cart.subtotalCents;
  const pixSubtotalCents = applyPixDiscount(cart.subtotalCents, pixDiscountPercent);

  return (
    <div>
      {cart.droppedCount > 0 && (
        <p style={{ color: "#b45309" }}>
          {cart.droppedCount} item(ns) saíram do carrinho por indisponibilidade.
        </p>
      )}

      <table>
        <thead>
          <tr>
            <th></th>
            <th>Produto</th>
            <th>Preço</th>
            <th>Quantidade</th>
            <th>Total</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          {cart.items.map((item) => (
            <tr key={item.variationId}>
              <td>
                {item.image ? (
                  <img
                    src={item.image}
                    alt={item.productName}
                    style={{ width: 60, height: 60, objectFit: "cover" }}
                  />
                ) : (
                  <div style={{ width: 60, height: 60, background: "#eee" }} />
                )}
              </td>
              <td>
                <Link href={`/produto/${item.productSlug}`}>{item.productName}</Link>
                <br />
                {item.size} - {item.color}
                {item.adjusted && (
                  <p style={{ color: "#b45309" }}>
                    Quantidade ajustada: só {item.availableStock} em estoque.
                  </p>
                )}
              </td>
              <td>R$ {centsToReais(item.priceCents)}</td>
              <td>
                <input
                  type="number"
                  min={1}
                  max={item.availableStock}
                  value={item.quantity}
                  onChange={(e) => {
                    const raw = Number(e.target.value);
                    const next = Number.isFinite(raw) ? Math.floor(raw) : 1;
                    updateItem(item.variationId, Math.min(Math.max(next, 1), item.availableStock));
                  }}
                  style={{ width: 60 }}
                />
              </td>
              <td>R$ {centsToReais(item.lineTotalCents)}</td>
              <td>
                <button type="button" onClick={() => removeItem(item.variationId)}>
                  Remover
                </button>
              </td>
            </tr>
          ))}
        </tbody>
      </table>

      <div style={{ marginTop: 24 }}>
        <p style={{ fontSize: 20, fontWeight: "bold" }}>
          Subtotal: R$ {centsToReais(cart.subtotalCents)}
        </p>
        {pixDiscountPercent > 0 && (
          <p>
            R$ {centsToReais(pixSubtotalCents)} no Pix ({pixDiscountPercent}% OFF)
          </p>
        )}
        {missingForFreeShipping > 0 ? (
          <p>Faltam R$ {centsToReais(missingForFreeShipping)} para ganhar frete grátis.</p>
        ) : (
          <p>Você ganhou frete grátis!</p>
        )}
        <p style={{ color: "#666" }}>O frete é calculado na próxima etapa.</p>
        <Link href="/checkout">
          <button type="button" style={{ padding: "12px 24px", marginTop: 8 }}>
            Finalizar compra
          </button>
        </Link>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Criar a página do carrinho**

Create `src/app/(loja)/carrinho/page.tsx`:
```tsx
import { prisma } from "@/lib/prisma";
import { CartView } from "./CartView";

export const dynamic = "force-dynamic";

export default async function CarrinhoPage() {
  const settings = await prisma.storeSettings.findUnique({ where: { id: 1 } });

  return (
    <div>
      <h1>Carrinho</h1>
      <CartView
        freeShippingCents={settings?.freeShippingCents ?? 0}
        pixDiscountPercent={settings?.pixDiscountPercent ?? 0}
      />
    </div>
  );
}
```

- [ ] **Step 3: Verificar manualmente o ciclo completo**

Run: `npm run dev`.

1. Com o carrinho vazio, acessar `/carrinho` — deve mostrar "Seu carrinho está vazio." e o link pra `/produtos`.
2. Adicionar 2 unidades de uma variação pela página do produto, voltar em `/carrinho` — deve listar o item com foto (ou o quadrado cinza), nome, tamanho/cor, preço unitário, quantidade 2, e o total da linha = preço × 2.
3. Alterar a quantidade pra 3 no campo — o total da linha e o subtotal devem atualizar.
4. Adicionar um segundo produto diferente e confirmar que o subtotal soma os dois.
5. Confirmar a linha de frete grátis: com subtotal abaixo do limite (padrão R$399,90) deve mostrar quanto falta; ultrapassando, deve mostrar "Você ganhou frete grátis!".
6. Clicar em "Remover" num item — deve sumir da lista, e o contador do cabeçalho deve baixar.
7. **Teste de segurança (importante):** com um item no carrinho, ir no admin (`/admin/produtos/[id]`) e **desativar** aquele produto (desmarcar "Ativo"). Voltar em `/carrinho` e recarregar — o item deve **sumir** e aparecer o aviso "1 item(ns) saíram do carrinho por indisponibilidade". Reativar o produto depois e confirmar que ele volta.
8. **Teste de estoque:** colocar no carrinho uma quantidade maior que o estoque disponível (ex: editar o estoque da variação pra 1 no admin com 3 unidades no carrinho). Recarregar `/carrinho` — a quantidade deve ser reduzida pro estoque disponível e aparecer o aviso "Quantidade ajustada: só 1 em estoque."

- [ ] **Step 4: Rodar a suíte de testes e o build de produção**

Run: `npm test`
Expected: PASS (todos os testes, incluindo os novos da Task 1)

Run: `npm run build`
Expected: build completa sem erros.

Depois do build, confirmar que `/carrinho` **não** está estaticamente pré-renderizado:
```bash
grep -o '"/[^"]*": {' .next/prerender-manifest.json
```
Expected: `/carrinho` NÃO deve aparecer na lista (só rotas legitimamente estáticas como `/_not-found`, `/favicon.ico`, `/login`).

- [ ] **Step 5: Commit**

```bash
git add src/app/\(loja\)/carrinho
git commit -m "feat: add cart page with server-validated prices and stock"
```

## Definição de pronto (Plano Carrinho)

- [ ] `npm run dev` sobe sem erros
- [ ] `npm run build` completa sem erros, e `/carrinho` não aparece em `.next/prerender-manifest.json`
- [ ] `npm test` passa (16 anteriores + os novos de `cart.test.ts`)
- [ ] Adicionar ao carrinho pela página do produto funciona, e o contador do cabeçalho atualiza na hora
- [ ] O carrinho sobrevive a um F5 (persistido no `localStorage`)
- [ ] `/carrinho` lista itens com preço, quantidade editável, remoção, e subtotal correto
- [ ] Progresso pro frete grátis aparece corretamente nos dois estados (falta X / ganhou)
- [ ] **Produto desativado no admin some do carrinho do cliente, com aviso**
- [ ] **Quantidade acima do estoque é reduzida automaticamente, com aviso**
- [ ] Nenhum preço exibido vem do `localStorage` — todos vêm do servidor
- [ ] Nenhum arquivo `.env.example` foi criado
