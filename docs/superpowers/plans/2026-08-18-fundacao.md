# Fundação do projeto — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ter o projeto Next.js rodando localmente, conectado a um banco de dados Postgres com o schema completo do e-commerce, com um usuário admin conseguindo logar e acessar uma rota `/admin` protegida.

**Architecture:** Aplicação Next.js (App Router, TypeScript) com Prisma ORM sobre PostgreSQL (Supabase). Autenticação via NextAuth.js (Credentials provider, sessão JWT — sem tabelas de sessão no banco). Este é o Plano 1 de uma série de 7; os próximos planos (catálogo no admin, vitrine pública, carrinho/frete, checkout/pagamento, pedidos no admin, conta do cliente) serão escritos após este ser concluído.

**Tech Stack:** Next.js 15 (App Router), TypeScript, Prisma, PostgreSQL (Supabase), NextAuth.js v5, bcryptjs, Vitest (testes unitários)

**Spec:** `docs/superpowers/specs/2026-08-18-ecommerce-v1-design.md`

## Global Constraints

- Banco de dados: PostgreSQL, acessado via Prisma — não usar outro ORM/driver
- Autenticação: NextAuth.js v5 com Credentials provider e sessão JWT (sem adapter de banco para sessões)
- Senhas: hash com bcryptjs, nunca texto puro
- Segredos (`DATABASE_URL`, `NEXTAUTH_SECRET`, etc): só em `.env` (nunca commitado — `.env` deve estar no `.gitignore`)
- `isAdmin: Boolean` no `User` é o único controle de acesso ao admin nesta fase (sem sistema de papéis)

## Pré-requisito manual (antes da Task 2)

Antes de rodar a Task 2, é necessário ter uma string de conexão com um banco PostgreSQL. Recomendação: criar um projeto gratuito em https://supabase.com, e pegar a "Connection string" (modo "URI") em Project Settings → Database. Essa URL vai para a variável `DATABASE_URL` no `.env`. Isso é uma ação manual (criar conta, copiar a URL) — não é um passo de código.

---

### Task 1: Inicializar o projeto Next.js

**Files:**
- Create: todo o scaffold gerado por `create-next-app` na pasta atual (`C:\Users\User\loja-roupas`)
- Create: `.gitignore` (gerado automaticamente pelo create-next-app, já ignora `node_modules`, `.next`, `.env*`)

**Interfaces:**
- Produces: projeto Next.js rodável via `npm run dev`, estrutura `src/app/`

- [ ] **Step 1: Rodar o create-next-app**

Run (dentro de `C:\Users\User\loja-roupas`):
```bash
npx create-next-app@latest . --typescript --app --tailwind --eslint --src-dir --import-alias "@/*" --use-npm --no-turbopack --yes
```

- [ ] **Step 2: Verificar que o projeto sobe**

Run: `npm run dev`
Expected: servidor sobe em `http://localhost:3000` sem erros no terminal. Pare o servidor (Ctrl+C) depois de confirmar.

- [ ] **Step 3: Confirmar que `.env` está no `.gitignore`**

Abrir `.gitignore` e confirmar que existe uma linha `.env*` ou `.env`. Se não existir, adicionar a linha `.env*.local` e `.env`.

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "chore: scaffold Next.js project"
```

---

### Task 2: Schema do banco de dados com Prisma

**Files:**
- Create: `prisma/schema.prisma`
- Create: `.env` (não commitado — contém `DATABASE_URL`)
- Create: `.env.example` (commitado — mesma estrutura sem valores reais)

**Interfaces:**
- Consumes: `DATABASE_URL` definida no `.env` (pré-requisito manual acima)
- Produces: cliente Prisma tipado (`@prisma/client`) com os modelos `User`, `Address`, `Category`, `Product`, `ProductVariation`, `Order`, `OrderItem`, `Shipment`, `StoreSettings`, e o enum `OrderStatus`

- [ ] **Step 1: Instalar o Prisma**

Run:
```bash
npm install prisma --save-dev
npm install @prisma/client
npx prisma init --datasource-provider postgresql
```

- [ ] **Step 2: Preencher `.env` com a connection string real**

Editar `.env` (criado pelo `prisma init`) e definir:
```
DATABASE_URL="<connection string do Supabase>"
```

- [ ] **Step 3: Criar `.env.example`**

```
DATABASE_URL="postgresql://user:password@host:5432/dbname"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"
```

- [ ] **Step 4: Escrever o schema completo**

Substituir o conteúdo de `prisma/schema.prisma` por:

```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id           String    @id @default(cuid())
  name         String?
  email        String    @unique
  passwordHash String
  phone        String?
  isAdmin      Boolean   @default(false)
  addresses    Address[]
  orders       Order[]
  createdAt    DateTime  @default(now())
}

model Address {
  id            String   @id @default(cuid())
  user          User?    @relation(fields: [userId], references: [id])
  userId        String?
  label         String?
  recipientName String
  street        String
  number        String
  complement    String?
  neighborhood  String
  city          String
  state         String
  zipCode       String
  createdAt     DateTime @default(now())
}

model Category {
  id       String     @id @default(cuid())
  name     String
  slug     String     @unique
  parent   Category?  @relation("CategoryToParent", fields: [parentId], references: [id])
  parentId String?
  children Category[] @relation("CategoryToParent")
  products Product[]
}

model Product {
  id          String             @id @default(cuid())
  name        String
  slug        String             @unique
  description String
  brand       String?
  active      Boolean            @default(true)
  images      String[]
  category    Category           @relation(fields: [categoryId], references: [id])
  categoryId  String
  variations  ProductVariation[]
  createdAt   DateTime           @default(now())
}

model ProductVariation {
  id         String      @id @default(cuid())
  product    Product     @relation(fields: [productId], references: [id])
  productId  String
  size       String
  color      String
  sku        String      @unique
  priceCents Int
  stock      Int         @default(0)
  image      String?
  orderItems OrderItem[]

  @@unique([productId, size, color])
}

enum OrderStatus {
  AWAITING_PAYMENT
  PAID
  PAID_STOCK_ISSUE
  SHIPPED
  DELIVERED
  CANCELED
}

model Order {
  id              String      @id @default(cuid())
  user            User?       @relation(fields: [userId], references: [id])
  userId          String?
  contactEmail    String
  status          OrderStatus @default(AWAITING_PAYMENT)
  shippingAddress Json
  shippingCents   Int
  totalCents      Int
  paymentMethod   String?
  mpPaymentId     String?     @unique
  items           OrderItem[]
  shipment        Shipment?
  createdAt       DateTime    @default(now())
  updatedAt       DateTime    @updatedAt
}

model OrderItem {
  id          String           @id @default(cuid())
  order       Order            @relation(fields: [orderId], references: [id])
  orderId     String
  variation   ProductVariation @relation(fields: [variationId], references: [id])
  variationId String
  quantity    Int
  priceCents  Int
}

model Shipment {
  id            String   @id @default(cuid())
  order         Order    @relation(fields: [orderId], references: [id])
  orderId       String   @unique
  carrier       String?
  trackingCode  String?
  labelUrl      String?
  melhorEnvioId String?
  status        String   @default("pending")
  createdAt     DateTime @default(now())
}

model StoreSettings {
  id                 Int @id @default(1)
  freeShippingCents  Int @default(39990)
  pixDiscountPercent Int @default(5)
  maxInstallments    Int @default(12)
}
```

- [ ] **Step 5: Rodar a migração**

Run: `npx prisma migrate dev --name init`
Expected: cria a pasta `prisma/migrations/`, aplica no banco, e gera o Prisma Client sem erros.

- [ ] **Step 6: Verificar que as tabelas existem**

Run: `npx prisma studio` — abre uma interface web local. Confirmar visualmente que todas as tabelas (User, Address, Category, Product, ProductVariation, Order, OrderItem, Shipment, StoreSettings) aparecem. Fechar depois (Ctrl+C).

- [ ] **Step 7: Commit**

```bash
git add prisma/schema.prisma prisma/migrations .env.example package.json package-lock.json
git commit -m "feat: add Prisma schema for core e-commerce data model"
```

(note: `.env` não deve aparecer no `git status` — se aparecer, pare e confirme o `.gitignore` antes de continuar)

---

### Task 3: Helper de senha (hash/verificação) com teste

**Files:**
- Create: `src/lib/password.ts`
- Test: `src/lib/password.test.ts`

**Interfaces:**
- Produces: `hashPassword(plain: string): Promise<string>`, `verifyPassword(plain: string, hash: string): Promise<boolean>`

- [ ] **Step 1: Instalar dependências de teste e bcryptjs**

Run:
```bash
npm install bcryptjs
npm install -D vitest
```

- [ ] **Step 2: Escrever o teste (deve falhar)**

Create `src/lib/password.test.ts`:
```typescript
import { describe, it, expect } from "vitest";
import { hashPassword, verifyPassword } from "./password";

describe("password helpers", () => {
  it("verifies a correct password against its hash", async () => {
    const hash = await hashPassword("senha-correta-123");
    expect(await verifyPassword("senha-correta-123", hash)).toBe(true);
  });

  it("rejects an incorrect password", async () => {
    const hash = await hashPassword("senha-correta-123");
    expect(await verifyPassword("senha-errada", hash)).toBe(false);
  });

  it("never stores the password in plain text", async () => {
    const hash = await hashPassword("senha-correta-123");
    expect(hash).not.toBe("senha-correta-123");
  });
});
```

- [ ] **Step 3: Adicionar script de teste no `package.json`**

Em `package.json`, dentro de `"scripts"`, adicionar:
```json
"test": "vitest run"
```

- [ ] **Step 4: Rodar o teste e confirmar que falha**

Run: `npm test`
Expected: FAIL com erro de módulo `./password` não encontrado (arquivo ainda não existe)

- [ ] **Step 5: Implementar**

Create `src/lib/password.ts`:
```typescript
import bcrypt from "bcryptjs";

const SALT_ROUNDS = 10;

export async function hashPassword(plain: string): Promise<string> {
  return bcrypt.hash(plain, SALT_ROUNDS);
}

export async function verifyPassword(plain: string, hash: string): Promise<boolean> {
  return bcrypt.compare(plain, hash);
}
```

- [ ] **Step 6: Rodar o teste e confirmar que passa**

Run: `npm test`
Expected: PASS nos 3 testes

- [ ] **Step 7: Commit**

```bash
git add src/lib/password.ts src/lib/password.test.ts package.json package-lock.json
git commit -m "feat: add password hashing helpers"
```

---

### Task 4: Seed — configurações padrão da loja e usuário admin

**Files:**
- Create: `prisma/seed.ts`
- Modify: `package.json` (bloco `"prisma": { "seed": ... }`)

**Interfaces:**
- Consumes: `hashPassword` de `src/lib/password.ts` (Task 3)
- Consumes: variáveis de ambiente `ADMIN_EMAIL`, `ADMIN_PASSWORD` (adicionadas ao `.env`)

- [ ] **Step 1: Adicionar credenciais do admin ao `.env` e `.env.example`**

Em `.env`, adicionar (com um email e senha reais que você vai usar pra logar):
```
ADMIN_EMAIL="seu-email@exemplo.com"
ADMIN_PASSWORD="uma-senha-forte-aqui"
```

Em `.env.example`, adicionar (sem valor real):
```
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
```

- [ ] **Step 2: Instalar runner do seed**

Run: `npm install -D tsx`

- [ ] **Step 3: Escrever o seed**

Create `prisma/seed.ts`:
```typescript
import { PrismaClient } from "@prisma/client";
import { hashPassword } from "../src/lib/password";

const prisma = new PrismaClient();

async function main() {
  const adminEmail = process.env.ADMIN_EMAIL;
  const adminPassword = process.env.ADMIN_PASSWORD;

  if (!adminEmail || !adminPassword) {
    throw new Error("Defina ADMIN_EMAIL e ADMIN_PASSWORD no .env antes de rodar o seed");
  }

  await prisma.storeSettings.upsert({
    where: { id: 1 },
    update: {},
    create: { id: 1 },
  });

  const passwordHash = await hashPassword(adminPassword);

  await prisma.user.upsert({
    where: { email: adminEmail },
    update: { passwordHash, isAdmin: true },
    create: {
      email: adminEmail,
      passwordHash,
      isAdmin: true,
      name: "Admin",
    },
  });

  console.log(`Seed concluído. Admin: ${adminEmail}`);
}

main()
  .catch((e) => {
    console.error(e);
    process.exit(1);
  })
  .finally(async () => {
    await prisma.$disconnect();
  });
```

- [ ] **Step 4: Registrar o comando de seed no `package.json`**

Adicionar ao `package.json` (nível raiz, fora de `"scripts"`):
```json
"prisma": {
  "seed": "tsx prisma/seed.ts"
}
```

- [ ] **Step 5: Rodar o seed**

Run: `npx prisma db seed`
Expected: imprime `Seed concluído. Admin: <email>` sem erro

- [ ] **Step 6: Verificar no Prisma Studio**

Run: `npx prisma studio`, abrir a tabela `User`, confirmar que existe 1 registro com `isAdmin = true` e `passwordHash` preenchido (não a senha em texto puro). Fechar depois.

- [ ] **Step 7: Commit**

```bash
git add prisma/seed.ts package.json package-lock.json
git commit -m "feat: add seed script for store settings and admin user"
```

---

### Task 5: Autenticação (NextAuth) e middleware de proteção do `/admin`

**Files:**
- Create: `src/lib/auth.ts`
- Create: `src/app/api/auth/[...nextauth]/route.ts`
- Create: `middleware.ts`
- Create: `src/app/login/page.tsx`
- Create: `src/lib/prisma.ts`

**Interfaces:**
- Consumes: `verifyPassword` de `src/lib/password.ts` (Task 3)
- Produces: `auth()` (server-side session helper), `signIn`/`signOut` (client-side, re-exportados de `next-auth/react` onde necessário)
- Consumes: `PrismaClient` singleton de `src/lib/prisma.ts`

- [ ] **Step 1: Instalar o NextAuth**

Run: `npm install next-auth@beta`

- [ ] **Step 2: Gerar o `NEXTAUTH_SECRET` e adicionar ao `.env`**

Run: `node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"`
Copiar a saída e adicionar ao `.env`:
```
NEXTAUTH_SECRET="<valor-gerado>"
NEXTAUTH_URL="http://localhost:3000"
```
E ao `.env.example` (sem valor real, só as chaves).

- [ ] **Step 3: Criar o singleton do Prisma Client**

Create `src/lib/prisma.ts`:
```typescript
import { PrismaClient } from "@prisma/client";

const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };

export const prisma = globalForPrisma.prisma ?? new PrismaClient();

if (process.env.NODE_ENV !== "production") {
  globalForPrisma.prisma = prisma;
}
```

- [ ] **Step 4: Configurar o NextAuth**

Create `src/lib/auth.ts`:
```typescript
import NextAuth from "next-auth";
import Credentials from "next-auth/providers/credentials";
import { prisma } from "@/lib/prisma";
import { verifyPassword } from "@/lib/password";

export const { handlers, auth, signIn, signOut } = NextAuth({
  session: { strategy: "jwt" },
  providers: [
    Credentials({
      credentials: {
        email: { label: "Email", type: "email" },
        password: { label: "Senha", type: "password" },
      },
      authorize: async (credentials) => {
        const email = credentials?.email as string | undefined;
        const password = credentials?.password as string | undefined;
        if (!email || !password) return null;

        const user = await prisma.user.findUnique({ where: { email } });
        if (!user) return null;

        const valid = await verifyPassword(password, user.passwordHash);
        if (!valid) return null;

        return { id: user.id, email: user.email, name: user.name, isAdmin: user.isAdmin };
      },
    }),
  ],
  callbacks: {
    jwt: ({ token, user }) => {
      if (user) token.isAdmin = (user as { isAdmin?: boolean }).isAdmin ?? false;
      return token;
    },
    session: ({ session, token }) => {
      if (session.user) (session.user as { isAdmin?: boolean }).isAdmin = token.isAdmin as boolean;
      return session;
    },
  },
  pages: {
    signIn: "/login",
  },
});
```

- [ ] **Step 5: Criar a rota de API do NextAuth**

Create `src/app/api/auth/[...nextauth]/route.ts`:
```typescript
import { handlers } from "@/lib/auth";

export const { GET, POST } = handlers;
```

- [ ] **Step 6: Criar o middleware de proteção do `/admin`**

Create `middleware.ts` (raiz do projeto):
```typescript
import { auth } from "@/lib/auth";
import { NextResponse } from "next/server";

export default auth((req) => {
  const isAdminRoute = req.nextUrl.pathname.startsWith("/admin");
  const isAdmin = (req.auth?.user as { isAdmin?: boolean } | undefined)?.isAdmin;

  if (isAdminRoute && !isAdmin) {
    const loginUrl = new URL("/login", req.nextUrl.origin);
    return NextResponse.redirect(loginUrl);
  }
});

export const config = {
  matcher: ["/admin/:path*"],
};
```

- [ ] **Step 7: Criar a página de login**

Create `src/app/login/page.tsx`:
```tsx
"use client";

import { useState } from "react";
import { signIn } from "next-auth/react";
import { useRouter } from "next/navigation";

export default function LoginPage() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [password, setPassword] = useState("");
  const [error, setError] = useState("");

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setError("");
    const result = await signIn("credentials", { email, password, redirect: false });
    if (result?.error) {
      setError("Email ou senha inválidos");
      return;
    }
    router.push("/admin");
  }

  return (
    <main style={{ maxWidth: 360, margin: "80px auto" }}>
      <h1>Login</h1>
      <form onSubmit={handleSubmit}>
        <input type="email" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} required />
        <input type="password" placeholder="Senha" value={password} onChange={(e) => setPassword(e.target.value)} required />
        {error && <p style={{ color: "red" }}>{error}</p>}
        <button type="submit">Entrar</button>
      </form>
    </main>
  );
}
```

- [ ] **Step 8: Testar manualmente**

Run: `npm run dev`. Acessar `http://localhost:3000/admin` sem estar logado → deve redirecionar pra `/login`. Logar com o email/senha do `.env` (`ADMIN_EMAIL`/`ADMIN_PASSWORD`) → deve ir pra `/admin` (mesmo que a página ainda dê 404, já que ela é criada na Task 6 — o importante aqui é confirmar que o redirect e o login funcionam).

- [ ] **Step 9: Commit**

```bash
git add src/lib/auth.ts src/lib/prisma.ts src/app/api/auth src/app/login middleware.ts package.json package-lock.json .env.example
git commit -m "feat: add NextAuth credentials login and admin route protection"
```

---

### Task 6: Dashboard placeholder do admin

**Files:**
- Create: `src/app/admin/layout.tsx`
- Create: `src/app/admin/page.tsx`

**Interfaces:**
- Consumes: `auth()` de `src/lib/auth.ts` (Task 5)

- [ ] **Step 1: Criar o layout do admin**

Create `src/app/admin/layout.tsx`:
```tsx
import { auth, signOut } from "@/lib/auth";
import { redirect } from "next/navigation";

export default async function AdminLayout({ children }: { children: React.ReactNode }) {
  const session = await auth();
  const isAdmin = (session?.user as { isAdmin?: boolean } | undefined)?.isAdmin;

  if (!isAdmin) {
    redirect("/login");
  }

  return (
    <div>
      <header style={{ display: "flex", justifyContent: "space-between", padding: 16, borderBottom: "1px solid #ddd" }}>
        <strong>Admin</strong>
        <form
          action={async () => {
            "use server";
            await signOut({ redirectTo: "/login" });
          }}
        >
          <button type="submit">Sair</button>
        </form>
      </header>
      <main style={{ padding: 16 }}>{children}</main>
    </div>
  );
}
```

- [ ] **Step 2: Criar a página do dashboard**

Create `src/app/admin/page.tsx`:
```tsx
export default function AdminDashboardPage() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>Bem-vindo ao painel administrativo. Produtos, estoque e pedidos serão adicionados nos próximos planos.</p>
    </div>
  );
}
```

- [ ] **Step 3: Testar manualmente o fluxo completo**

Run: `npm run dev`. Repetir o teste da Task 5 Step 8: acessar `/admin` deslogado → redireciona pra `/login`; logar com as credenciais do admin → chega no dashboard, vendo "Bem-vindo ao painel administrativo"; clicar em "Sair" → volta pra `/login`, e tentar acessar `/admin` de novo deve redirecionar outra vez.

- [ ] **Step 4: Rodar a suíte de testes completa**

Run: `npm test`
Expected: PASS (os 3 testes da Task 3 continuam passando)

- [ ] **Step 5: Commit**

```bash
git add src/app/admin
git commit -m "feat: add protected admin dashboard placeholder"
```

---

## Definição de pronto (Plano 1)

- [ ] `npm run dev` sobe sem erros
- [ ] `npm test` passa
- [ ] Login com o usuário admin seedado funciona
- [ ] `/admin` redireciona pra `/login` quando deslogado, e mostra o dashboard quando logado como admin
- [ ] `.env` não aparece em nenhum commit (`git log --all --full-history -- .env` não retorna nada)
